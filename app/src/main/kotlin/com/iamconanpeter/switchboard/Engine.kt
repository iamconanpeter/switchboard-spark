package com.iamconanpeter.switchboard

class Engine {
    fun flip(rows: Int, cols: Int): Pair<Int, Int> {
        // deterministic placeholder: return seed of flip
        return Pair(rows, cols)
    }
}
